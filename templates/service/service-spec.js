describe('<%= "glx"+_.capitalize(name) %>', function() {

  beforeEach(module('<%= appname %>'));

  it('should ...', inject(function(<%= "glx"+_.capitalize(name) %>) {

	//expect(<%= _.camelize(name) %>.doSomething()).toEqual('something');

  }));

});