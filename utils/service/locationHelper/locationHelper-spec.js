describe('glxLocationHelper', function() {

  beforeEach(module('glxUtils'));

  it('should ...', inject(function(glxLocationHelper) {

	//expect(glxLocationHelper.doSomething()).toEqual('something');

  }));

});